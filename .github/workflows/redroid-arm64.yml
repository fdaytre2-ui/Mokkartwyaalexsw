name: 🚀 RM64 workflow for Samsung A15

on:
  workflow_dispatch:

jobs:
  arm-redroid:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360

    steps:
      - name: ✅ Environment Check
        run: |
          echo "Architecture: $(uname -m)"
          echo "CPUs: $(nproc)"
          echo "RAM: $(free -h)"

      - name: 🔧 Install Dependencies
        run: |
          sudo apt update
          sudo apt install -y docker.io adb unzip wget curl jq lsof psmisc
          sudo systemctl enable --now docker
          sudo chmod 666 /var/run/docker.sock
          sudo apt install -y linux-modules-extra-$(uname -r)
          sudo modprobe binder_linux devices=binder,hwbinder,vndbinder || true

      - name: 📥 Download GApps + Build Image
        run: |
          wget https://github.com/MindTheGapps/14.0.0-arm64/releases/download/MindTheGapps-14.0.0-arm64-20250203_200051/MindTheGapps-14.0.0-arm64-20250203_200051.zip -O gapps.zip
          cat > Dockerfile << 'EOF'
          FROM redroid/redroid:14.0.0-latest AS redroid_base
          FROM ubuntu:22.04 AS extractor
          RUN apt-get update && apt-get install -y unzip sed
          ADD gapps.zip /gapps.zip
          RUN mkdir -p /tmp/gapps && unzip /gapps.zip -d /tmp/gapps
          COPY --from=redroid_base /system /tmp/redroid_system
          COPY --from=redroid_base /vendor /tmp/redroid_vendor
          RUN sed -i 's/ro.hardware=redroid/ro.hardware=mt6789/g' /tmp/redroid_vendor/build.prop || true
          RUN sed -i 's/ro.product.board=/ro.product.board=mt6789/g' /tmp/redroid_vendor/build.prop || true
          RUN sed -i 's/ro.board.platform=/ro.board.platform=mt6789/g' /tmp/redroid_vendor/build.prop || true
          RUN find /tmp/redroid_system /tmp/redroid_vendor /tmp/gapps -type f -name "*.prop" -exec sed -i 's/redroid14_arm64/SM-A155F/g' {} + || true
          RUN find /tmp/redroid_system /tmp/redroid_vendor /tmp/gapps -type f -name "*.prop" -exec sed -i 's/redroid_arm64/a15nsxx/g' {} + || true
          RUN find /tmp/redroid_system /tmp/redroid_vendor /tmp/gapps -type f -name "*.prop" -exec sed -i 's/redroid/samsung/gi' {} + || true
          FROM redroid/redroid:14.0.0-latest
          COPY --from=extractor /tmp/gapps/system/ /system/
          COPY --from=extractor /tmp/redroid_system/ /system/
          COPY --from=extractor /tmp/redroid_vendor/ /vendor/
          EOF
          sudo docker build -t redroid-a15-arm .

      - name: 📱 Start Samsung A15
        timeout-minutes: 8
        run: |
          sudo docker run -itd --privileged --name samsung_a15 \
            -v ~/redroid_a15_data:/data \
            -p 5555:5555 \
            redroid-a15-arm \
            ro.sf.lcd_density=396 \
            ro.hardware=mt6789 \
            ro.boot.hardware=mt6789 \
            ro.product.board=mt6789 \
            ro.board.platform=mt6789 \
            ro.product.model=SM-A155F \
            ro.product.brand=samsung \
            ro.product.manufacturer=samsung \
            ro.product.device=a15 \
            ro.build.fingerprint=samsung/a15nsxx/a15:14/UP1A.231005.007/A155FXXU1AXA2:user/release-keys \
            ro.radio.noril=no \
            ro.baseband=MOLY.LR12A.R3.TC01.PI.V2 \
            ro.secure=1 \
            ro.debuggable=0
          sleep 40
          adb connect localhost:5555 || true
          sudo docker restart samsung_a15
          sleep 10
          adb connect localhost:5555 || true

      - name: 🔌 Open Direct ADB Tunnel & Start Stream
        run: |
          set +e
          sudo lsof -t -i:8000 | xargs -r sudo kill -9
          set -e
          
          wget -q https://github.com/ekzhang/bore/releases/download/v0.5.1/bore-v0.5.1-aarch64-unknown-linux-musl.tar.gz
          tar -xzf bore-v0.5.1-aarch64-unknown-linux-musl.tar.gz
          chmod +x bore
          ./bore local 5555 --to bore.pub > /tmp/bore.log 2>&1 &
          
          curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
          sudo apt-get install -y nodejs
          git clone https://github.com/NetrisTV/ws-scrcpy.git
          cd ws-scrcpy && npm install && npm start &
          
          sleep 15
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-arm64 -O cloudflared
          chmod +x cloudflared
          ./cloudflared tunnel --url http://localhost:8000 2>&1 | tee /tmp/cf.log &
          
          sleep 10
          echo "========================================================="
          echo "Stream URL: $(grep -o 'https://.*trycloudflare.com' /tmp/cf.log | head -1)"
          echo "ADB Connect: $(grep -Eo 'bore.pub:[0-9]+' /tmp/bore.log | head -1)"
          echo "========================================================="
          sleep 20000
